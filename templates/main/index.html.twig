{% extends 'base.html.twig' %}


{% block title %}Acceuil{% endblock %}

{% block body %}

<div class="container text-center">
    <div class="title">
    <br>
    <h1 class="text-dark">LES  <img src="{{asset('image/logo.jpg') }}" width="60" height="60"> CHAMBRES  </h1>



       
		{% for item in chambres %}
			<br><div class="row g-2 text-center "><br><br>
				<div class="card bg-dark" >
				<h5 class="card-title">{{ item.titre }}</h5>
					<img src="{{ item.photo }}" height="400" width="400" class="card-img-top">
					<div class="card-body">
						
						<p class="card-text">{{ item.descriptioncourte|raw }}</p>
                        <p></p>
						<p class="card-text">{{ item.prixJour / 100|number_format(2) }}€</p> 
					
					</div>
                    <a class="btn btn-success mx-auto " href="{{ path('app_show', {id: item.id})}}">pour en savoir plus</a>
				</div>
			</div><br>
			
		{% endfor %}
		
	
    


{% endblock %}
